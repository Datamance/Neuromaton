# -*- coding: utf-8 -*-
from setuptools import setup, find_packages

setup(
    name='Neuromaton',
    version='0.1',
    packages=find_packages(),
    include_package_data=['neuromaton'],
    long_description=open('README').read(),
    install_requires=[],
)
